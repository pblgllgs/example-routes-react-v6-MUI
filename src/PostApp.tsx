import AppRouter from './router/AppRouter';
import { AppTheme } from './theme';

const PostApp = () => {
    return (
        <AppTheme>
            <AppRouter />
        </AppTheme>
    );
};

export default PostApp;
